export class CardMarket {
    private url: string;
    private updatedAt: string;
    private prices: string;

    constructor(
        url: string,
        updatedAt: string,
        prices: string
    ) { }
}